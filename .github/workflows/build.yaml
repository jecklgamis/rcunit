name: build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout rcunit
        uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y autoconf automake libtool build-essential

      - name: Build and install logmoko
        run: |
          git clone https://github.com/jecklgamis/logmoko.git
          cd logmoko
          ./autogen.sh
          ./configure
          make
          sudo make install
          sudo ldconfig

      - name: Run autogen
        run: ./autogen.sh

      - name: Configure
        run: ./configure

      - name: Build
        run: make all

      - name: Run tests
        run: make check
